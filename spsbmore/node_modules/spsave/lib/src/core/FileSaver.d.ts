/// <reference types="bluebird" />
import * as Promise from 'bluebird';
import { IAuthOptions } from 'sp-request';
import { ICoreOptions, IFileContentOptions } from './SPSaveOptions';
export declare class FileSaver {
    private static saveConfilctCode;
    private static cobaltCode;
    private static directoryNotFoundCode;
    private static fileDoesNotExistOnpremCode;
    private static fileDoesNotExistOnlineCode;
    private static reUploadTimeout;
    private static maxAttempts;
    private sprequest;
    private uploadFileRestUrl;
    private getFileRestUrl;
    private checkoutFileRestUrl;
    private checkinFileRestUrl;
    private updateMetaDataRestUrl;
    private path;
    private foldersCreator;
    private logger;
    private coreOptions;
    private file;
    constructor(coreOptions: ICoreOptions, credentialOptions: IAuthOptions, fileOptions: IFileContentOptions);
    save(): Promise<any>;
    private saveFile(requestDeferred, attempts?);
    private skipUpload(deferred);
    private updateMetaData(data);
    private checkinFile();
    private checkoutFile();
    private getFileByUrl();
    private tryReUpload(exception, deferred, attempts);
    private buildRestUrls();
}
