var spsave = require('./../lib/src/index').spsave;
var fs = require('fs');

    var opts  = {
      username: 'administrator',
      password: 'QazWsx123',
      domain: 'sp',
      siteUrl: 'http://sp2013dev/sites/dev/',
      glob: ['lib/src/core/*.js', '!./node_modules/**/*'],
      folder: 'SiteAssets',
      notification: true
    };

function save(opts){
  spsave(opts)
  .then(function (data) {
    console.log('success');
  })
  .catch(function(err){
    if(err.statusCode){
    console.log('WOW error((: ' + err.statusCode);
    } else {
      console.log(err);
    }
  });
}

save(opts);
